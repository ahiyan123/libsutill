[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "flexutils"
version = "0.1.0"
description = "Eliminates repetitive tasks, whitespace, and case headaches."